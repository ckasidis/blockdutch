{
  "name": "hardhat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "help": "hardhat help",
    "node": "dotenv -- hardhat node",
    "test": "hardhat test",
    "test:report-gas": "REPORT_GAS=true hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv-cli": "^7.3.0",
    "hardhat": "^2.18.2"
  }
}
